# dist 는 build.ps1 이 매번 새로 만들어내는 결과물이라 저장할 필요가 없습니다.
# GitHub 서버도 스스로 만들어서 배포합니다.
dist/

# 주간 정리 글을 쓸 때만 쓰는 임시 글감입니다.
# prepare-weekly-context.ps1 이 매번 새로 만듭니다.
weekly/.work/

# 윈도우가 폴더마다 자동으로 만드는 파일
Thumbs.db
desktop.ini
