# 2026-08-22 (토) — 비트뉴스 작업 기록

앞선 기록은 `worklog/2026-08-21-b.md` 에 있습니다.

---

## 0. 오늘의 결과 한 줄

**밀려 있던 주간 정리(8월 3주차)를 써서 발행했고**, 세 사이트 SEO 점검을 함께 했습니다.
점검에서 나온 개선 목록 중 **2번(robots.txt 에 feed.xml)까지 처리**했습니다(아래 6번).
나머지는 목록으로 정리해뒀습니다.

> 이날 novacent 폴더에서는 1번(프리렌더)을 처리하고 배포까지 마쳤습니다.
> 그쪽 내용은 `projects\novacent\worklog\2026-08-22.md` 49~50번에 있습니다.

---

## 1. 주간 정리 8월 3주차 발행

### 왜 기간이 9일인가

마지막 발행이 **8/12(2주차, 8/5~8/12)** 였고 오늘이 8/22 라 한 주가 통째로 비어 있었습니다.
그런데 빠진 기간의 흐름이 고르지 않았습니다.

```
8/13 ~ 8/19   6만3천 → 6만5천 달러   거의 횡보
8/20 ~ 8/21   6만5천 → 7만6천 달러   +18% 급등  ← 진짜 사건
```

원래 주기대로 3주차를 8/13~8/19 로 끊으면 **조용한 주만 다루고 급등은 빠집니다.**
사용자에게 세 가지 안을 제시했고 **"8/13~8/21 한 번에"** 를 골랐습니다.

> ⚠️ 그래서 3주차만 9일짜리입니다. **다음 글은 8/22 부터 시작해야 합니다.**
> 4주차를 8/20 부터 잡으면 이 글과 이틀이 겹칩니다.

### 다룬 것

| 섹션 | 내용 |
|---|---|
| 1. 시세 | 6만3379 → 7만6522달러. 상승분이 마지막 이틀에 몰렸다는 점을 표로 보임 |
| 2. 왜 올랐나 | 미 재무부 장기국채 바이백 확대(8/19 발표)가 방아쇠. 코스피도 같은 날 5.89% 급등 |
| 3. 숏스퀴즈 | 숏 청산 27억4000만 달러 = 2021년 집계 이후 최대. 전체 청산 32억 달러 |
| 4. ETF·심리 | ETF 3거래일 연속 순유입, 공포탐욕 29 → 46 → 62 |
| 5. 국내 | **개정 특금법 8/20 시행** — 신고 심사 강화, 1000만원 이상 해외 송금 심사 |

**5번을 넣은 이유:** 앞 네 개는 전부 해외 시세 이야기입니다.
한국 독자가 실제로 영향을 받는 건 특금법 쪽이라 별도 섹션으로 뺐습니다.

### 숫자 검증

지시서(`.github/weekly-instructions.md`)대로 **본문의 모든 수치를 `full.md` 원문에서 확인**했습니다.
확인 목록은 `weekly/.work/meta.json` 의 `verified` 에 20줄로 남겼습니다. (`.work` 는 gitignore 라 로컬에만 있음)

**판단이 필요했던 두 건:**

1. **숏 청산액이 매체 집계마다 달랐습니다.** 27억4000만(14:01 블록미디어) vs 28억9000만(16:35 같은 매체 팟캐스트).
   같은 매체의 시각 차이라 범위로 쓰지 않고 **27억4000만 한 값만 쓰고 전체 청산 32억 달러를 병기**했습니다.
2. **"한 주 만에 20% 넘게 상승"(id c489422d62)은 쓰지 않았습니다.** 매체의 기준 기간이 이 글의 집계 기간과 달라서,
   본문에서는 실제 시세브리핑 값으로만 서술했습니다.

### 발행 절차 (다음에도 이대로)

```powershell
.\prepare-weekly-context.ps1 -Days 10        # 글감 뽑기 (.work 를 지우고 새로 만듦)
#  → weekly/posts/<slug>.html 과 weekly/.work/meta.json 을 손으로 작성
.\apply-weekly-meta.ps1 -Slug 2026-08-w3 -From 2026-08-13 -To 2026-08-21
.\build.ps1                                   # 로컬 확인용
git add weekly/posts/<slug>.html weekly/weekly.json
git commit / git push                         # push 하면 Actions 가 빌드·배포
```

> **`weekly.json` 을 직접 고치지 마세요.** `apply-weekly-meta.ps1` 이 주석을 보존하며 끼워 넣습니다.
> 잘못 등록했으면 `git checkout weekly/weekly.json` 으로 되돌리고 meta.json 고쳐서 다시 실행하면 됩니다.
> (오늘 설명문이 167자로 규칙 155자를 넘어서 실제로 이 방법으로 되돌렸습니다 → 150자로 수정)
>
> **`data/articles.json` 은 커밋하지 마세요.** 로컬 빌드가 이 파일을 건드리는데,
> 저장은 Actions 가 합니다. 빌드 후 `git checkout data/articles.json` 으로 되돌리세요.

### 배포 검증 (3단계)

| 단계 | 결과 |
|---|---|
| 1단계 · 내용 대조 | 라이브와 로컬 dist 를 줄 단위 비교 → **다른 22줄이 전부 자동 생성되는 속보·관련기사 목록.** 본문은 완전 동일 ✅ |
| 2단계 · 주소 응답 | 글 200 / 목록 반영 / sitemap 등록 / 본문 근거 링크 8건 전부 200 ✅ |
| 3단계 · 진짜 화면 | 헤드리스 Edge 1280px 캡처 — 표 2개·설명상자 3개·경고상자·고지문 정상, 치환 안 된 자리표시자 0건 ✅ |

주소: https://coinfocus.co.kr/weekly/2026-08-w3.html

---

## 2. 알아둘 것 — 관련기사 목록이 기간을 이틀 넘어갑니다

글 아래 **"이 기간의 주요 기사 12건"** 이 8/22 새벽 기사를 보여줍니다.
기간은 8/13~8/21 로 넣었는데도 그렇습니다.

**원인:** 기간 필터는 UTC 기준인데 화면 표시는 한국시간입니다.
8/22 02:42 KST = 8/21 17:42 UTC 라 필터에는 걸립니다.

w2 에서는 관련기사가 전부 마지막 날(8/12)에 몰려 있어 눈에 안 띄었습니다.
**고장이 아니라 원래 그런 동작**이고, 이번엔 자정을 넘겨서 드러났을 뿐입니다.
급하지 않아 오늘은 손대지 않았습니다. → 개선 목록에 넣어뒀습니다.

---

## 3. CLAUDE.md 에서 사실과 다른 부분

`CLAUDE.md` 에 **"이 폴더는 git 저장소가 아니다"** 라고 적혀 있는데 **사실이 아닙니다.**

```
origin  https://github.com/cho87880148-dotcom/bitnews.git
브랜치  main
```

`.git/` 이 있고 push 하면 Actions 가 빌드·배포합니다. 오늘도 그렇게 발행했습니다.
(git 이 없는 건 옆 폴더 `test` 와 `novacent` 입니다)

**오늘은 문구를 고치지 않았습니다** — 주간 정리 작업 범위 밖이라서요. 개선 목록에 넣어뒀습니다.

---

## 4. SEO 점검 결과 (요약)

사용자 요청으로 bnguide / coinfocus / novacent 세 곳을 실측 점검했습니다.
**coinfocus 는 기본기가 잘 되어 있습니다** — robots·sitemap·canonical·og·구조화 데이터·리다이렉트 전부 정상.

이 사이트에서 나온 개선 후보:

| 우선 | 내용 |
|---|---|
| 중 | `robots.txt` 에 **feed.xml 선언이 없습니다.** 네이버는 RSS 를 수집 경로로 씁니다 (bnguide 는 되어 있음) |
| 중 | 사이트맵 2,192개 중 **목록 페이지 166개**(`page-N.html`)가 내용 없이 크롤링 예산만 씁니다 |
| 중 | 첫 화면 **뉴스 썸네일 46장이 `alt=""`**, 게다가 이미지를 tokenpost.kr 에서 직접 끌어옵니다 |
| 하 | HSTS 없음 — GitHub Pages 라 헤더를 못 넣습니다 (구조를 바꾸지 않는 한 방법 없음) |
| 하 | 설명문 80자 초과 (bybit 수수료 90자, 주간정리·뉴스 150자대) — 네이버 검사 경고 기준 |
| 하 | `/weekly/index.html` 만 구조화 데이터 없음 |
| 하 | 거래소 가이드 21장에 이미지 0장 |

세 사이트 통합 목록은 **아티팩트로 따로 정리**했습니다. 내일 이걸 보고 진행합니다.

---

## 5. 다음에 할 일

- [ ] **4주차 주간 정리는 8/22 부터** 시작 (3주차가 8/21 까지라 겹치지 않게)
- [ ] 위 SEO 개선 목록 진행 (내일)
- [ ] `CLAUDE.md` 의 "git 저장소가 아니다" 문구 수정

---

## 6. SEO 정비 2번 — robots.txt 에 feed.xml 추가

점검에서 나온 개선 목록의 2번 항목입니다.

### 무엇이 문제였나

`feed.xml` 은 원래부터 잘 만들고 있었습니다(항목 30건, 200 응답).
그런데 **robots.txt 에 적어두지 않아 검색엔진에게 알려주지를 못하고 있었습니다.**

```
전:  Sitemap: https://coinfocus.co.kr/sitemap.xml
후:  Sitemap: https://coinfocus.co.kr/sitemap.xml
     Sitemap: https://coinfocus.co.kr/feed.xml      ← 추가
```

**네이버는 sitemap 뿐 아니라 RSS 도 수집 경로로 씁니다.**
이 사이트는 뉴스가 1시간마다 올라오는데 sitemap 만 알려주면 새 기사를 늦게 가져갑니다.
옆 폴더 `test`(bnguide)는 처음부터 둘 다 적어뒀는데 여기만 빠져 있었습니다.

### 고친 곳

`build.ps1` 의 10번 구역(검색엔진용 파일). `robots.txt` 는 파일로 두는 게 아니라
빌드가 만들어내므로 **`dist/robots.txt` 를 직접 고치면 다음 빌드에 날아갑니다.**

```powershell
$sitemapLine = "Sitemap: $baseUrl/sitemap.xml`nSitemap: $baseUrl/feed.xml"
```

### 확인

| 항목 | 결과 |
|---|---|
| BOM 유지 (한글 주석 있어 필수) | `ef bb bf` ✅ |
| 구문 검사 (`Parser::ParseFile`) | 오류 0건 ✅ |
| 빌드 후 `dist/robots.txt` | Sitemap 2줄 ✅ |
| `feed.xml` | 항목 30건 정상 ✅ |

> 참고 — 이번 빌드 로그에 **"주간 정리가 링크한 기사 2건은 상한을 넘겨도 보존했습니다"**
> 가 떴습니다. 어제 쓴 3주차 글이 근거로 링크한 기사가 `$KeepMax` 상한에 밀려
> 사라지는 것을 build.ps1 이 막아준 것입니다. 링크가 깨지지 않습니다.
