# 작업 일지 — 2026년 8월 21일 (금)

작업 폴더: `C:\Users\user\projects\newhome` (비트뉴스 / coinfocus.co.kr)
주제: **광고 승인용 임시 상호(Novacent) 되돌리기 — 완료**

---

## 0. 오늘의 결과 한 줄

첫 화면 상호를 `Novacent` → `비트뉴스` 로 되돌리고, 푸터의 사업자 정보 줄을 지웠다.
**실제 사이트에 반영된 것까지 확인했다.**

---

## 1. 왜 했나

2026-08-06 에 광고 승인용으로 걸어둔 임시 조치였다. 승인 절차가 끝나서
사용자가 원상복구를 지시했다. 되돌리는 법은 `worklog/2026-08-06.md` 3번에 적혀 있었고
그대로 따랐다.

---

## 2. 고친 것

### `build.ps1` 237~238줄

```powershell
# 이전
$tempHomeBrand   = 'Novacent'
$tempHomeBizInfo = 'Company : Novacent | CEO : Hyunho Jo | Business Registration Number : 186-14-02804'

# 이후
$tempHomeBrand   = ''
$tempHomeBizInfo = ''
```

`templates/base.html` 은 예고대로 **손대지 않았다.** `{{BRAND}}` 가 자동으로
`sources.json` 의 `비트뉴스` 를 쓰고, `{{BIZINFO}}` 는 빈 줄이 된다.

### `build.ps1` 224~235줄 — 주석 정리

"되돌려야 할 임시 조치" 안내였는데 다 되돌렸으므로, **"지금은 꺼져 있다 / 다시 켜려면"**
쪽으로 문구를 바꿨다. 장치 자체는 남겨뒀다 — 나중에 또 쓸 일이 있을 수 있고,
코드를 지우는 것보다 두 줄을 비워두는 편이 안전하다.

### `CLAUDE.md` — "★ 되돌려야 할 임시 변경이 걸려 있다" 절 삭제

그 절 자체가 "되돌린 뒤 지울 것" 이라고 적고 있었다.

### 손대지 않은 것

`sources.json` 의 `site.name`, `og:site_name`, RSS 제목, 구조화 데이터 — 애초에
Novacent 로 바꾼 적이 없다.

---

## 3. 실측 확인 (실제 사이트 https://coinfocus.co.kr/)

| 확인 항목 | 결과 |
|---|---|
| `<title>` | `비트뉴스 — 비트코인 뉴스 한눈에` ✅ |
| 로고 | `₿비트뉴스` ✅ |
| 푸터 사업자 정보 (`186-14-02804`) | **0건** ✅ |
| 페이지 전체에서 `Novacent` | **0건** ✅ |
| 푸터 남은 줄 | `마지막 업데이트 … · © 2026 비트뉴스` (원래 있던 줄, 정상) ✅ |
| 헤드리스 Edge 스크린샷 | 로고 `비트뉴스` 로 보임 ✅ |
| 빌드 결과 `dist/` 전체 `Novacent` | **0건** ✅ |

빌드는 `.\build.ps1` 로 로컬에서도 한 번 돌려 확인했다(기사 2000건 / 목록 167쪽).

---

## 4. 배포

`git push` 두 번. GitHub Actions 가 빌드해서 자동 반영됐고, 위 3번은
**push 후 실제 사이트를 curl 로 다시 받아 확인한 값**이다.

- `cc6fb60` 첫 화면 임시 상호·사업자 정보 제거
- `76e66a8` 주석·CLAUDE.md 정리

작업 전 `git pull` 했고, `build.ps1` 이 건드린 `data/articles.json` 은
관례대로 `git checkout --` 로 되돌려 봇에게 맡겼다.
두 번째 push 때 봇 커밋과 겹쳐서 `git pull --rebase` 로 얹었다.

---

## 5. 남은 것

없다. 임시 조치는 완전히 정리됐다.
